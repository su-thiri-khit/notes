import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    titleText: {
        fontWeight: '300',
        fontSize: 20,
        paddingBottom: 4
    },
    bodyText: {
        fontWeight: '300',
        fontSize: 16,
    }
});