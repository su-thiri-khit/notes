import React from 'react';
import { View, Text, SafeAreaView } from 'react-native'

const CreateNoteScreen= (props: any) => {
    return(
        <SafeAreaView style={{ flex: 1 }}>
            <Text>All Notes Screen</Text>
        </SafeAreaView>
    )
}

export default CreateNoteScreen